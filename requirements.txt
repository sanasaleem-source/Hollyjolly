# AI Production Studio — Dependencies

# Core
pyyaml>=6.0.1
pydantic>=2.5.3
PyQt6>=6.6.1
requests>=2.31.0
pytest>=7.4.3

# Cloud AI provider (text, vision, image) — QtMultimedia ships bundled
# inside the main PyQt6 wheel; "PyQt6-Multimedia" is not a real PyPI
# package and was removed. google-genai replaces the now end-of-life
# (sunset Aug 31 2025) google-generativeai SDK.
google-genai>=0.3.0

# Local AI provider — text/vision (HuggingFace)
huggingface_hub>=0.20.3
transformers>=4.38.0
torch>=2.2.0
accelerate>=0.27.0
sentencepiece>=0.1.99

# Local AI provider — image generation (Stable Diffusion family)
diffusers>=0.27.0

# Scene / timeline interchange
usd-core>=23.11
opentimelineio>=0.15.0
